# 최소 필수 의존성만 포함 (Python 3.13 호환)

# Flask 웹 프레임워크
Flask>=3.0.0
flask-cors>=4.0.0
flask-limiter>=3.5.0
Werkzeug>=3.0.0
limits>=3.0.0

# 웹서버 (프로덕션용)
gunicorn>=21.2.0

# PDF 변환 (선택적)
pdfkit>=1.0.0
weasyprint>=60.0

# HTTP 클라이언트
requests>=2.31.0

# 환경 변수
python-dotenv>=1.0.0

# AI API 모듈 의존성
httpx>=0.24.0
pydantic>=2.0.0
PyYAML>=6.0.2
Pillow>=9.0.0
asyncio-throttle>=1.0.0
tenacity>=8.0.0
tiktoken>=0.4.0

# AI Provider SDK
google-genai>=0.6.0
openai>=1.35.0
anthropic>=0.31.0

# 최소 필수 의존성만 포함 (Python 3.13 호환)

# Flask 웹 프레임워크
Flask>=3.0.0
flask-cors>=4.0.0
flask-limiter>=3.5.0

# 웹서버 (프로덕션용)
gunicorn>=21.2.0

# PDF 변환 (선택적)
pdfkit>=1.0.0

# HTTP 클라이언트
requests>=2.31.0

# AI API 의존성 (최신 버전)
google-generativeai>=0.3.0
openai>=1.3.0
anthropic>=0.7.0

# 환경 변수
python-dotenv>=1.0.0